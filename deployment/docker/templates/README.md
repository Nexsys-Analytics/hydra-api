# Why this folder

This templates folder contains the templates to be used inside the parent folder.

# Pipfile

This file will contain eventual env variables to be replaced before applying it with the install.sh command 
