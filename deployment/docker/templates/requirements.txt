#############################################################################
# NOTE:
#
# CURRENT_REPOSITORY_BRANCH = The current repository actual branch:
#       In this way it is possible rebuilding the image without changing branch from the developing one
#
# <REPOSITORY>_BRANCH_FOR_DIST = The branch selected from the config.json relatively to the current DIST (dev, test, live, etc)
####################################################################################à

packaging
git+https://github.com/arskom/spyne.git@spyne-2.13.2-alpha
git+https://github.com/hydraplatform/hydra-base.git@${HYDRA_BASE_BRANCH_FOR_DIST}
git+https://github.com/hydraplatform/hydra-server.git@${HYDRA_SERVER_BRANCH_FOR_DIST}
git+https://gitlab+deploy-token-19:vUZf73-ALze_9y3ARNFa@gitlab.hydra.org.uk/hydra/hydra-api.git@${CURRENT_REPOSITORY_BRANCH}
git+https://github.com/pywr/hydra-pywr-common.git@${HYDRA_PYWR_COMMON_BRANCH_FOR_DIST}
